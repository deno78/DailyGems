import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.dailygems',
  appName: 'DailyGems',
  webDir: 'dist/DailyGems/browser'
};

export default config;

export const environment = {
  production: true,
  admob: {
    testMode: false,
    appId: '', // Set this in your environment variables or build configuration
    adUnitIds: {
      banner: '', // Set this in your environment variables or build configuration
      interstitial: '', // Set this in your environment variables or build configuration
      rewarded: '' // Set this in your environment variables or build configuration
    }
  }
};